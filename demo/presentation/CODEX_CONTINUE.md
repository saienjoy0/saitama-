# 発表デモv2.2の引き継ぎ

ユーザー「実装していいよ」でv2.1の実装を承認。その後「続けてください」で、小5向けの具体的な手順、電気代探検、子のごほうび提案（今月500円/毎月108円増額/ディズニー券）を追加指定。実装済み。計画へ戻して承認を求めない。

## 今の状態

- DEMO02 / v2.2、独立した合成発表デモ。製品のDESIGN/D90とprototype/gakusta/index.htmlは維持。
- 入口: src/PresentationApp.tsx。src/Quest.tsx、src/family.ts、src/ui.tsx、src/presentation.css。
- v1のsrc/App.tsx/state.ts/style.cssは参照として残り、main.tsxからは使わない。旧ブラウザテストは実行対象外。playwright.config.tsはv2-flow.spec.tsを対象にする。
- npm run build成功。状態/計算14テスト成功。文書検査成功。v2ブラウザ14ケースは列挙のみ。
- Cloud Browserがhttp://127.0.0.1:4173をERR_BLOCKED_BY_CLIENTで拒否。別の制御手段やURLでの回避は行わない。目視・E2E・スクリーンショット・発表所要時間は未確認。
- release/presentation-offline.htmlはビルドから保存する単体配布物。変更後はnpm run buildで作り直し、同じ内容で更新する。

## 次に進める作業

1. READMEの3分台本と分岐を読む。今回の価値の中心は「子が疑問を持つ→やることを具体化→比べる→本人の言葉で発見」。
2. ブラウザが利用可能な正規の環境でnpm run test:browserを実行。PC・スマホ、200%文字、キー操作、モーション抑制、写真入力を目視する。
3. テスト/表示の不具合が出た箇所だけ修正し、build・状態テスト・関係するブラウザケースを確認。
4. 発表操作を実測し、README/DEMO_STATEの未確認を実測結果に更新。実画像を使うサンプルは架空素材だけ。
5. 同じ専用ブランチへ保存。本体実装の承認、mainマージ、公開、ライブAI接続へ拡大しない。

## 変えてはいけないつながり

写真は任意。ポイントと共有は独立。本人が非共有なら祖父母へ表示しない。承認版と新聞素材を確認し、撤回が既存号にも反映される。ポイント二重加算/二重消費不可。ごほうびは条件も親が承認し、申請中の変更は不可。
歩数の更新と元気ボタンの日時は別。歩数共有OFFや未取得でも安否連絡できる。健康推定なし。
実AIは未接続。模擬AIは本人が選んだ疑問の種類に対応する固定手順。本人の答えや承認を自動生成しない。
